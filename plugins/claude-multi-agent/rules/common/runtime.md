# ランタイムルール

## 必須

- **npm / npx 使用禁止**: このプロジェクトでは bun をランタイムとして使用する
- **パッケージインストール**: `bun install` / `bun add <package>`
- **スクリプト実行**: `bun run <script>`
- **パッケージ実行**: `bunx <package>`（npx の代わり）
- **テスト実行**: `bun test`
